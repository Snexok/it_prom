Для запуска frontend в dev режиме используйте команды:

<code>cd front</br>
npm run dev</code>

Для запуска backend в dev режиме используйте команды:

<code>cd back</br>
./mvnw spring-boot:run</code>
